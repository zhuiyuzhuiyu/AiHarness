# pre-verify

Run before validation starts.

Suggested responsibilities:

- Build the verification plan from changed files
- Prefer targeted checks before broad suites
- Add browser verification when UI flows changed
- Add extra checks for migrations or security changes
