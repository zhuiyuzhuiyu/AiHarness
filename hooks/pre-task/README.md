# pre-task

Run before a task starts.

Suggested responsibilities:

- Resolve issue context from URL or ticket ID
- Confirm spec directory exists
- Check that required project docs were loaded
- Detect if the task is high-risk and requires approval
